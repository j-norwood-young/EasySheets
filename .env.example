# Host port for the app (docker-compose)
# PORT=3000

# SQLite database path (file path or file: URL; in Docker use file:/data/sqlite.db with the data volume)
# DB_PATH=file:/data/sqlite.db

SECRET_KEY=R4ND0M$TR!NG
